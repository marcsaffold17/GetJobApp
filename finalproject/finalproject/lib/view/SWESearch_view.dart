import 'package:flutter/material.dart';
import 'homepage.dart';

class SWESearch extends StatelessWidget {
  const SWESearch({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Center(
        child: Container(),
      ),
    );
  }
}
